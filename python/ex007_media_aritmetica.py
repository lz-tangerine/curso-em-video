firstNote = float(input('Digite a primeira nota do aluno: '))
secondNote = float(input('Digite a segunda nota do aluno: '))
average = (firstNote + secondNote) / 2
print('A média entre {:.1f} e {:.1f} é igual a {:.1f}'.format(firstNote, secondNote, average))
